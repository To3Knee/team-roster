
{
  "name": "team-contact-roster",
  "version": "1.3.3",
  "description": "Team Contact Roster (Business Card Sized) — offline desktop app",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . TeamContactRoster --overwrite --platform=win32 --arch=x64 --icon=icon.ico --prune=true --out=dist --ignore=\"dist|node_modules|\\.git\""
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-packager": "^17.1.2"
  }
}
